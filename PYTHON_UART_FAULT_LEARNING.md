# Python UART 错误请求注入学习笔记

## 1. Round 5C 做了什么

Stage 9 Round 5 分为三个逐步深入的测试：

- Round 5A：发送一次正确请求，验证基本协议链路。
- Round 5B：连续发送 20 次正确请求，验证短时间连续通信稳定性。
- Round 5C：故意发送损坏请求，验证 STM32 拒绝错误并恢复。

Round 5C 不修改 STM32 固件，只新增 PC 端 Python 测试脚本。脚本先确认合法请求能够得到正确图像，然后依次发送五种完整错误帧。每个错误帧后再发送合法请求，检查状态机能否恢复。最后还会发送一条截断帧，验证等待超过 100 ms 后能够重新接收合法请求。

## 2. 什么是错误注入

错误注入是主动修改协议中的某个字段，用来模拟非法请求或传输异常。例如把正确版本号 1 改为 2，或者把正确 CRC 的某一位翻转。

它的目的不是破坏开发板，而是检查软件是否具备以下能力：

1. 识别错误。
2. 丢弃错误帧。
3. 不产生未定义输出。
4. 重新同步到下一条合法帧。

本轮只修改应用层请求字节，不制造电气故障，也不修改 DMA 或 FreeRTOS 配置。

## 3. 为什么错误请求要保持静默

当前二进制请求协议没有定义 ACK、NACK 或二进制错误响应。因此 STM32 收到错误二进制帧时应保持串口静默，只更新内部统计并丢弃错误帧。

保持静默很重要：

- 文本 `ERROR` 会污染二进制客户端的接收缓冲。
- 客户端可能把错误文本误认为 OV56RGB5 图像的开头。
- 错误帧不能触发 DUMP，否则会错误地产生一帧图像。
- 下一条合法请求仍应正常工作。

脚本把“错误请求后 0.4 秒内收到 0 B”作为静默成功条件。即使只收到 1 B，也会判定静默失败。

## 4. 五种错误分别是什么

脚本测试以下五种完整错误帧：

### version 错误

把协议版本从 `0x01` 改成 `0x02`。STM32 应记录版本错误并丢弃本帧。

### msg_type 错误

把 `REQUEST_IMAGE` 的消息类型从 `0x20` 改成 `0x21`。STM32 应记录类型错误。

### payload_len 错误

v1 请求要求 `payload_len=0`，测试帧把它改为 1，但仍只发送固定 14 B，不追加 payload。STM32 应在解析长度字段时识别错误。

### CRC 错误

version、msg_type、seq、payload_len 和 EOF 都保持合法，只翻转 CRC 的最低一位。STM32 应记录 CRC 错误。

### EOF 错误

body 和 CRC 都保持合法，只把正确的 `EOF0=0x0D` 改为 `0x00`。STM32 应记录帧尾错误。

## 5. 为什么 version、type、length 错误仍要重新计算 CRC

错误注入应尽量只破坏一个目标字段。

例如 version 从 1 改为 2 后，如果仍沿用原 version 的 CRC，这条帧会同时包含 version 错误和 CRC 错误，测试者就难以判断 STM32 首先识别了哪一种问题。

脚本先用目标字段构造 body，再重新计算 CRC：

```python
body = struct.pack("<BBHH", version, msg_type, seq, payload_len)
crc = zlib.crc32(body) & 0xFFFFFFFF
```

因此 version、type 和 length 测试中的 CRC 都与修改后的 body 一致，目标错误只有对应字段本身。

## 6. CRC 错误怎么制造

正确 CRC 计算完成后，脚本执行：

```python
crc ^= 0x00000001
```

符号 `^` 表示按位异或。与 1 异或会翻转最低位：原来是 0 就变成 1，原来是 1 就变成 0。

这种方法只破坏 CRC 的一位，不改变 body、SOF 或 EOF，能够明确测试 STM32 的 CRC 校验分支。

## 7. 静默检查怎么实现

`expect_silence()` 先清除串口旧数据，再发送错误请求：

```python
ser.reset_input_buffer()
ser.write(request)
ser.flush()
```

随后等待 0.4 秒并读取当前收到的全部字节：

```python
time.sleep(0.4)
received = ser.read_all()
```

如果 `len(received) == 0`，表示等待期间没有文本、图像或其他字节，静默检查通过。

如果收到数据，脚本只显示前 64 B 的十六进制内容，既能帮助定位问题，也不会把整帧 38400 B payload 打印到终端。

## 8. 为什么错误后还要发送合法请求

只证明 STM32 能拒绝错误请求还不够。错误可能让协议状态机停留在错误状态，导致后续所有合法请求都无法识别。

因此每种错误之后，脚本都会：

1. 等待 0.1 秒。
2. 发送一条新的合法 14 B 请求。
3. 接收完整 38426 B OV56RGB5 图像。
4. 检查 header、尺寸、payload 长度和 CRC。

合法恢复请求成功，才能说明错误帧被丢弃后协议重新同步。请求 seq 只标识 PC 请求，不要求等于响应 frame_id。

## 9. 截断帧是什么

完整请求应为 14 B，截断测试只发送：

```text
A5 5A 02
```

- `A5 5A` 让分发器进入二进制候选状态。
- `02` 是错误 version。
- 后面的 11 B 不再发送。

它可以模拟 PC 程序发送到一半退出、串口线缆中断或应用层数据没有完整写出。

## 10. 为什么等待 0.3 秒

STM32 的错误尾部隔离超时为 100 ms。version 错误出现后，固件会暂时丢弃本应属于该错误帧的剩余尾部，避免尾部字节被误当成新文本或新二进制帧。

截断帧没有剩余尾部，所以脚本等待：

```python
DISCARD_TIMEOUT_WAIT_SECONDS = 0.3
```

300 ms 明显大于 100 ms。等待结束后再发送合法请求，可以验证隔离状态已经自动解除。

## 11. 为什么隔离超时不增加 timeout 计数

截断测试在收到 `0x02` 时已经确定是 version 错误，并立即记录一次 `VERSION_ERROR`。

后续 100 ms 超时只负责退出“丢弃错误帧尾部”的隔离状态，不代表又出现了一条新的半帧解析错误，因此不应重复增加 `binary_request_timeout_count`。

从统计值为 0 的全新固件开始运行时，截断帧预计增加一次 version 错误，而不是一次 version 错误再加一次 timeout 错误。若开发板已有统计，应观察增量，不要求绝对值从 0 开始。

## 12. 脚本总体流程

```text
先发送一个合法请求
    ↓
确认基础链路正常
    ↓
发送 VERSION_ERROR 并检查静默
    ↓
发送合法请求并检查恢复
    ↓
依次测试 TYPE、LENGTH、CRC、EOF
    ↓
发送截断帧 A5 5A 02
    ↓
等待 0.3 秒并检查静默
    ↓
发送最后一条合法请求并检查恢复
    ↓
统一输出汇总和 PASS/FAIL
```

脚本总共发送 7 条合法请求、5 条完整错误请求和 1 条截断错误帧。某一项失败后仍继续剩余测试，以便观察后续是否能够恢复。

## 13. PASS 代表什么

Round 5C 的最终 `PASS` 必须同时满足：

- 基线合法请求成功。
- 五种完整错误帧均没有产生任何串口输出。
- 五种错误后的合法请求均恢复成功。
- 截断帧等待期间没有串口输出。
- 截断帧等待超过 100 ms 后合法请求恢复成功。
- 所有恢复图像都是 38426 B。
- 所有恢复图像的 OV56RGB5 header 正确。
- 所有恢复图像的 payload CRC 正确。

任一条件失败，最终结果都是 `FAIL`，程序退出码为 1。

## 14. PASS 不能证明什么

本轮通过也不能覆盖所有故障场景：

- 没有测试真实电气噪声。
- 没有测试运行中拔插串口。
- 没有制造真正的 UART 或 DMA 硬件错误。
- 没有制造 StreamBuffer 溢出。
- 没有进行几小时或几天的长期运行。
- 没有测试多个 PC 程序同时占用 COM4。
- UART TX 仍然是阻塞发送方式。

错误注入验证的是应用层协议字段检查和重同步行为，不应夸大成完整硬件可靠性认证。

## 15. 面试时怎么解释

可以这样回答：

> 在正常请求和连续请求通过后，我又使用 Python 做了协议错误注入。分别构造错误 version、消息类型、载荷长度、CRC 和帧尾的请求。STM32 对错误帧保持静默，只更新内部错误统计，不触发 DUMP。每个错误帧后再发送合法请求，验证状态机能够重新同步。另外发送截断帧并等待超过 100 ms，验证尾部隔离超时后可以恢复。

Python 只是 PC 测试工具。真正负责 UART DMA 接收、StreamBuffer 缓冲、协议解析、错误隔离和 FreeRTOS 任务处理的是 STM32 固件。

## 16. 本轮新增的 Python 知识

### 函数默认参数

```python
def build_request(seq, version=0x01, corrupt_crc=False):
```

调用者不传参数时使用默认值，只在某项错误测试中覆盖目标字段。

### 布尔值

`True` 表示条件成立，`False` 表示条件不成立，例如 `corrupt_crc=True` 表示需要制造 CRC 错误。

### bytes

`bytes` 表示不可变字节数据。下面代码生成两个 EOF 字节：

```python
bytes((eof0, eof1))
```

### 按位异或

`^` 对整数逐位异或，本轮用它翻转 CRC 的一位。

### 多返回值

```python
return success, frame_id, elapsed_ms, error_message
```

调用者可以一次接收多个结果，分别判断是否成功、显示帧号、耗时和错误原因。

### 计数器与最终条件

每成功或失败一次就对相应计数器加 1。最后使用多个 `and` 条件判断是否所有项目都通过。

## 17. Stage 9 Round 5 总结

- Round 5A 完成单次合法二进制请求和 OV56RGB5 校验。
- Round 5B 完成连续 20 次合法请求、成功率和耗时统计。
- Round 5C 完成五种错误帧、静默行为和错误后恢复验证。

三部分共同验证 PC 到 STM32 的 UART 应用层协议：正常请求能够工作，连续请求能够保持稳定，错误请求能够被拒绝并重新同步。

Stage 9 Round 5 的 Python 脚本用于产生测试输入和检查输出；STM32 端 UART DMA、StreamBuffer、协议状态机、图像采集与 FreeRTOS 任务仍然是项目核心。
