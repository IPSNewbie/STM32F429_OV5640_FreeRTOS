\# TEST\_LOG.md



\## OV5640 输出尺寸稳定性测试



\### 测试目标



当前尝试将 OV5640 图像从原来的 320x240 扩展到更大的 LCD 显示区域。



由于当前工程采用：



OV5640 -> DCMI -> DMA -> LCD GRAM



也就是 DCMI DMA 直接写 LCD，不经过 RAM framebuffer，因此不能直接对 320x240 图像做软件缩放。



\### 已测试分辨率



| OV5640 输出尺寸 | LCD 显示现象 | 结论 |

|---|---|---|

| 320x240 | 正常 | 原稳定路径正常 |

| 400x240 | 雪花 | 宽度超过 320 后不稳定 |

| 480x240 | 雪花 | 宽度超过 320 后不稳定 |

| 320x320 | 正常 | 高度扩展到 320 可行 |

| 400x300 | 雪花 | 宽度超过 320 后不稳定 |

| 480x320 | 雪花 | 暂时不可用 |



\### 关键结论



当前测试结果表明：



\- 320 宽输出稳定。

\- 高度从 240 增加到 320 仍然可以正常显示。

\- 只要输出宽度超过 320，LCD 就会出现雪花纹。

\- 单独修改 `0x3824` 降低 PCLK 没有解决 480x320 雪花问题。

\- 只修改 `0x3808\~0x380B` 不能可靠实现 480x320 输出。



\### 当前阶段采用方案



当前先采用稳定方案：



\- OV5640 输出 320x320 RGB565。

\- LCD 横屏 480x320。

\- 图像显示在 LCD 中间。

\- 左右各保留约 80 像素黑边。

\- 显示窗口为：



```c

Camera\_DCMI\_StartToLCD(80, 0, 320, 320);

