### 阶段性调试结果

当前测试结果表明：

* OV5640 输出宽度为 320 时，当前 DCMI -> DMA -> LCD GRAM 直写路径可以稳定显示。
* 高度从 240 增加到 320 仍然可以正常显示。
* 当输出宽度超过 320 时，例如 400x240、480x240、400x300、480x320，LCD 出现雪花纹。
* 单独修改 `0x3824` 尝试降低 PCLK，测试值 `0x04`、`0x08`、`0x0C`、`0x10`、`0x18` 均未解决雪花问题。
* 当前只修改 `0x3808~0x380B` 的方法，不能可靠实现 480x320 输出。

### 重要说明

320x320 居中显示只是阶段性调试结果，不是最终目标。

当前最终任务仍然是：

* 实现 OV5640 图像铺满 3.5 寸 NT35310 LCD。
* 目标显示尺寸为 480x320。
* 显示结果应无雪花、无滚动、无花屏、无明显错位。

因此，不能把 320x320 居中显示作为当前任务的完成状态。

### 320x320 阶段性可用路径

当前发现 320x320 可以稳定显示，因此它可以作为调试回退路径：

* OV5640 输出 320x320 RGB565。
* LCD 横屏 480x320。
* 图像显示在 LCD 中间。
* 左右各保留约 80 像素黑边。
* 显示窗口为：

```c
Camera_DCMI_StartToLCD(80, 0, 320, 320);
```

该路径仅用于阶段性验证和回退，不代表“铺满 LCD”任务完成。

### 480x320 当前问题

480x320 全屏显示目前仍未完成。

已知问题：

* 480x320 TestBar 显示雪花。
* 480x320 RealImage 显示花屏/滚动。
* 400x240、480x240 也出现雪花，说明问题和“输出宽度超过 320”高度相关。
* 当前只改输出宽高寄存器 `0x3808~0x380B` 不足以形成稳定的 480x320 输出。
* 单独调整 `0x3824` 不能解决问题。

### 下一步计划

下一步不进入画质调参。

当前继续围绕“铺满 LCD”任务进行：

1. 读取正点原子完整摄像头实验工程：

```text
D:\MCU+FreeRTOS\STM32_HAL\ISP_Project\实验38 摄像头实验
```

2. 搜索并分析完整 OV5640 输出尺寸 / 输出窗口设置函数，例如：

```text
OV5640_OutSize_Set
OV5640_ImageWin_Set
OV5640_Set_Window
OV5640_SetOutputSize
OV5640_RGB565_Mode
```

3. 移植必要的窗口、裁剪、缩放和输出尺寸设置函数。

4. 新增稳定的 480x320 TestBar 路径。

5. 先验证 480x320 TestBar 全屏稳定显示。

6. 再验证 480x320 RealImage 全屏显示。

7. 在 480x320 全屏显示稳定之前，不开始曝光、白平衡、亮度、对比度、饱和度、锐度等画质调参。
